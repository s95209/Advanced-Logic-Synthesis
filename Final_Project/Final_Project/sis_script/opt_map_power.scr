sweep; eliminate -1
simplify 
eliminate -1

sweep; eliminate 5
simplify 

resub -a

gkx -abt 30
resub -a; sweep
gcx -bt 30
resub -a; sweep

gkx -abt 10
resub -a; sweep
gcx -bt 10
resub -a; sweep

gkx -ab
resub -a; sweep
gcx -b
resub -a; sweep

eliminate 0
decomp -g *

eliminate -1; sweep

stg_to_network
read_library /users/student/mr111/lywu22/ALS/Final_Project/sis_script/synch.genlib
map -m 0
power_estimate




